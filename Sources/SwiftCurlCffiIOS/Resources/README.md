# Generated Resources

Run `scripts/build-all.sh` to generate:

- `curl_cffi_ios_payload.zip`
- `manifest.json`

Those generated files are intentionally ignored by Git until a reproducible
artifact exists.

